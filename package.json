{
  "name": "fetch-hdi-articles",
  "version": "1.0.0",
  "description": "Fetch all HDI articles from the PensionPro dev documentation site",
  "scripts": {
    "fetch": "node fetch-hdi-articles.js",
    "install-browsers": "npx playwright install chromium"
  },
  "dependencies": {
    "playwright": "^1.44.0"
  }
}
